# test-npm-commands.ps1

**Purpose:**
Tests all npm commands from `package.json`, logs issues to the nearest `claude.md`, and updates status.

**Usage:**
```
pwsh ./test-npm-commands.ps1
```

**Notes:**
- Automates validation of all npm scripts and logs failures for review.
