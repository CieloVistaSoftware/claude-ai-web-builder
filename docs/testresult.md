change website to darkmode
add a left nav layout
 - with a color theme picker a color bar that changes the theme of the template shown in main
 - Add a drop down to select left nav, right nav, and topnave layouts, all should have a header, main, and footer section
 - add a drop down of different web site types
the header says "Claude WebSite Builder
- the embedded claude ai should be able to generate an example web site based on the questions asked in the chat client. Which is shown in the main part of the page, this main part will have it's own layout and will include a Header, Navigator, Main and footer section. This content will be the generated web site that can be saved and hosted on its own.  Each web site must have a claude ai interrface.
- Once it builds the website it must show it in main.