{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "wb-btn.schema.json",
  "title": "wb-btn",
  "description": "A simple button web component for WB demos.",
  "type": "object",
  "properties": {
    "variant": {
      "type": "string",
      "description": "Button style variant (primary, secondary, warning, danger, success)"
    },
    "disabled": {
      "type": "boolean",
      "description": "Whether the button is disabled"
    }
  },
  "required": []
}
