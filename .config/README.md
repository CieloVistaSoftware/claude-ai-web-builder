# .config Directory

This directory contains configuration files for the WB framework project.

## Contents
- `playwright.config.*` - Playwright test configuration files
- `jsconfig.json` - JavaScript project configuration
- `config.js` - Main configuration file
- `config.schema.json` - Configuration schema validation

## Purpose
Centralized location for all project configuration files to keep the root directory clean.